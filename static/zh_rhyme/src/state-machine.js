


var ProgressButtonList = {
    currentButtonIndex: 0,

    initialize: function() {
        var self = this;
        for (var i = 0;i < slef.length; i += 1) {
            self.getButtonByIndex(i).progressValue = 0;
        }
    },

    transition: function(event) {
        // switch(this)
    }
}